# TopBand BMS Gateway - ESP32-S3 partition table (8 MB flash)
#
# Layout:
#   nvs       24 KB   configuration storage (NVS keys)
#   otadata    8 KB   OTA rollback state
#   app0     3072 KB  active firmware slot (A)
#   app1     3072 KB  OTA firmware slot (B)
#   spiffs   1920 KB  LittleFS - UI assets, chart history, alerts
#   coredump  64 KB   panic crash dump
#
# Compare to ESP32 default_8MB.csv which ships with Arduino ESP32 core.
# This table keeps the two 3 MB app slots (so V3.0 firmware has room to grow
# from the current V2.67.2 ~1.3 MB footprint) and reserves 1.9 MB for
# LittleFS, which is large enough for HTML/CSS/JS UI bundle (~200 KB) plus
# 48 h chart history at 3 min resolution (~40 KB) plus headroom.

# Name,     Type, SubType,  Offset,    Size,     Flags
nvs,        data, nvs,      0x9000,    0x6000,
otadata,    data, ota,      0xf000,    0x2000,
app0,       app,  ota_0,    0x10000,   0x300000,
app1,       app,  ota_1,    0x310000,  0x300000,
spiffs,     data, spiffs,   0x610000,  0x1E0000,
coredump,   data, coredump, 0x7F0000,  0x10000,
