# ─── Local-only design documents ───
# These contain architectural reasoning and Claude Code phase prompts
# that are intentionally not part of the public surface.
docs/architecture.md
prompts/

# Local-only development notes (not for public repo)
CLAUDE.md
docs/CLAUDE_CODE_SESSION_TEMPLATES.md
docs/V3_0_REARCHITECTURE_PROMPT.md
docs/V3_0_BOOTSTRAP_PROMPT.md
docs/V3_0_SETUP_GUIDE.md
docs/CODE_REVIEW_V2.66.md
docs/CODE_REVIEW_V3.1_FULL.md

# ─── Build artifacts ───
.pio/
.pioenvs/
.piolibdeps/
build/
build_host/
sdkconfig.esp32s3
sdkconfig.*.old
compile_commands.json
include/git_sha.h

# Build output binaries (project root and build dirs only — fixtures are tracked)
/*.o
/*.elf
/*.bin
/*.hex
/*.sha256
build_host/**/*.o
build_host/**/*.elf

# ─── IDE / editor ───
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/extensions.json.example
.idea/

# ─── Python (tools/) ───
__pycache__/
*.pyc
*.pyo

# ─── OS junk ───
.DS_Store
Thumbs.db

# ─── Backups and scratch ───
*.bak
*.swp
*.tmp
scratch/

# ─── Web UI build artifacts (regenerated by web/build_ui.py at pio run time) ───
web/littlefs_ui.tar
web/littlefs_ui.tar.gz
web/ui/ui_version.txt
# Generated header — never tracked; build_ui.py creates it before compilation.
src/storage/ui_provisioner.h

# ─── Dependency managers ───
node_modules/

# ─── Release checksums (kept as GitHub Release assets only, not tracked) ───
# NOTE: release-notes-v*.md is intentionally NOT ignored here — those files
# are not actually uploaded as GitHub Release assets (only pasted into the
# release body text), so they still need to live in the repo. See PR/commit
# for chore/untrack-release-notes for the gap this leaves.
SHA256SUMS-v*.txt
# Local Claude sync helpers (not for upstream)
claude_sync_overview.txt
overview.sh
