#pragma once
// Host stub: nvs_store.cpp includes this header but calls no symbol from it
// (nvs_flash_init() etc. are called elsewhere, e.g. main.cpp, which is not
// part of this test target). Present only so the #include resolves.
